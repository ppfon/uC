/* Uma variável simples pode ser interpretada como um vetor contendo um único
 * elemento e, portanto, pode ser passada por referência se usarmos a notação
 * de vetor. Com base nessa idéia, codifique uma rotina minmax(t,min,max) que
 * retorne através dos parâmetros min e max, as temperaturas mínima e máxima, 
 * respectivamente, armazenadas no vetor t.
 */

#include <stdio.h>


