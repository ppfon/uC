/* Dados os coeficientes a0, a1, a2, ..., an de um polinômio 
 * p(x) = a0xn + a1xn-1 +...+ an-1x + an,e uma sequência de valores de x 
 * (flag: x = 0), avaliar o polinômio p(x) para cada um dos valores entrados
 */

#include <stdio.h>
#define FLAG 0

int
main(void)
{
        float coeficientes[];

        for (i = 0; ; i++) {
                printf("Entre com o coeficiente de posição %d:\n", i);

